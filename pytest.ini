[pytest]
testpaths = tests
# -q keeps the output readable; -ra prints a summary line for every skipped or
# failed test, so a silently skipped database test can never go unnoticed.
addopts = -q -ra
markers =
    db: needs a live PostgreSQL through the SSH tunnel (bash scripts/tunnel-start.sh)
